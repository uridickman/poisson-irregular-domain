[build-system]
requires = [
    "setuptools>=68"
]
build-backend = "setuptools.build_meta"

[project]
name = "p2d"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.0.0,<=2.4.4",
    "matplotlib>=3.8",
    "numba>=0.60",
    "scipy>=1.17"
]